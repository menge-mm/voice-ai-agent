# Voice AI Agent - Python Dependencies
# Only direct dependencies needed by the application

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# OpenAI Integration
openai==1.10.0

# Text-to-Speech (Coqui XTTS)
TTS==0.22.0

# Deep Learning (CPU version - see note below for GPU)
torch==2.1.2
torchaudio==2.1.2

# Audio Processing
soundfile==0.12.1

# Data Models & Validation
pydantic==2.5.3

# Utilities
python-dotenv==1.0.1

# Note: numpy, scipy are automatically installed as dependencies of torch/TTS
# Note: For GPU support, install PyTorch with CUDA first:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
