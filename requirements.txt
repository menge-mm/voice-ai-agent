# Voice AI Agent - Python Dependencies
# Using LATEST versions and MAINTAINED libraries (2025)

# Web Framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# AI & ML - HuggingFace Ecosystem (LATEST)
transformers==4.47.1
accelerate==1.2.1
openai==1.57.2

# Deep Learning (PyTorch - LATEST, Python 3.11/3.12 compatible)
torch==2.5.1
torchaudio==2.5.1

# Audio Processing
soundfile==0.12.1
scipy==1.14.1
librosa==0.10.2

# Data Models & Validation (Pydantic v2 - LATEST)
pydantic==2.10.3

# Utilities
python-dotenv==1.0.1

# Optional: For GPU support with CUDA 12.1+
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# Note: This uses HuggingFace transformers pipeline for TTS (NOT discontinued Coqui)
# Supported TTS models: Bark, SpeechT5, Parler-TTS
